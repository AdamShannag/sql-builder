package builder

const (
	GREATER_THAN = iota
	GREATER_THAN_OR_EQUAL
	SMALLER_THAN
	SMALLER_THAN_OR_EQUAL
	EQUAL_TO
	NOT_EQUAL_TO
)

const (
	_select = "SELECT"
	from    = "FROM"
	where   = "WHERE"
	and     = "AND"
	or      = "OR"
)
